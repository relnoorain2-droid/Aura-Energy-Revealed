name = "auralis-coach"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets are NOT stored here. Set them from the terminal:
#   wrangler secret put OPENAI_API_KEY
#   wrangler secret put APP_SHARED_TOKEN   (optional)
